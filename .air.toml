root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/looker ."
  bin = "./tmp/looker"
  include_ext = ["go"]
  exclude_dir = ["tmp", "frontend", "node_modules"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
